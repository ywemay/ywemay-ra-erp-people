import { TopToolbar, } from "react-admin";
import CreateContactButton from "../../buttons/CreateContact";

export default function Actions() {
  return <TopToolbar>
    <CreateContactButton />
  </TopToolbar>
}